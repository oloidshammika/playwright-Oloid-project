name: Playwright Tests - Oloid Folder with Ortoni Report
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
      - name: Install dependencies
        run: npm ci
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Playwright tests for Oloid_test folder
        # This step runs tests, generating ortoni-data-history.sqlite
        run: npx playwright test Oloid_test
        continue-on-error: true

      - name: Upload Ortoni Report Artifact
        uses: actions/upload-artifact@v4
        if: ${{ always() }}
        with:
          name: ortoni-report-artifact
          path: ortoni-report/
          if-no-files-found: warn
          compression-level: 6

      - name: Archive Playwright Test Results
        uses: actions/upload-artifact@v4
        if: ${{ always() }}
        with:
          name: playwright-test-results
          path: playwright-report/

  deploy-report:
    needs: test
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Ortoni CLI
        # Installs the main 'ortoni-report' package which includes the CLI
        run: npm install ortoni-report

      - name: Setup Pages
        uses: actions/configure-pages@v5

      # Download artifact contents into a temporary location
      - name: Download Ortoni Report Artifact
        uses: actions/download-artifact@v4
        with:
          name: ortoni-report-artifact
          path: /tmp/ortoni-download

      # ✅ FINAL FIX: Uses $GITHUB_WORKSPACE and corrects the nested path for the artifact content.
      - name: Generate HTML Report from SQLite Data
        run: |
          # Define standard paths using the $GITHUB_WORKSPACE variable for stability
          DEPLOY_DIR="$GITHUB_WORKSPACE/ortoni-report"

          # The downloaded artifact includes a nested folder named 'ortoni-report'
          DOWNLOAD_DIR="/tmp/ortoni-download/ortoni-report" 
          DB_FILE="$DOWNLOAD_DIR/ortoni-data-history.sqlite"

          # 1. Ensure the target deployment directory exists
          mkdir -p "$DEPLOY_DIR"

          # 2. Copy the database file from the downloaded artifact location 
          #    This copies the SQLite file into the deployment folder.
          cp "$DB_FILE" "$DEPLOY_DIR/"

          # 3. Run the generator from the deployment folder using the full path.
          # The CLI will now find the .sqlite file in the DEPLOY_DIR and generate ortoni-report.html.
          npx ortoni-report show-report --dir "$DEPLOY_DIR" --file ortoni-report.html --port 3000

      - name: Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ortoni-report/

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
